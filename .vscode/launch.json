{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "request": "launch",
            "name": "PW-DEBUG",
            "skipFiles": ["<node_internals>/**"],
            "command": "./node_modules/.bin/playwright test --config=playwright.config.ts --project='Desktop Chrome' --workers 4 --retries=0",
            "env": {
                "TAG": "@C1",
                "CI": "false",
            }
        }
    ]
}
